/*Istall all the modules like express, morgan,mongoose and create a object name for all these modules. */
//Connection of the mongodb database
//Using a varible and assigning it a link that is copied from the mongodb and use the module mongoose.connect 
//that has then and catch where then function had call back that return when the database is connected.
//contect this file to routers using app.use(router filename)
//using the Methods app.get we can redirect or render as a callback function to home.